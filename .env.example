# 本项目为纯静态站点。本地开发默认不需要环境变量。
#
# —— 构建（Vite / CI）——
# BASE_PATH=/                 # 自有域名根路径（默认本地也是 /）
# BASE_PATH=/jiangfeng-blog   # GitHub 项目页；CI 的 deploy.yml 会自动设置
#
# —— 站点规范 URL（改代码，不改 env）——
# src/config/site.ts → site.url
# scripts/generate-rss.mjs → site.url（保持与上面一致）
# 当前预览：https://laijiangfeng.github.io/jiangfeng-blog
# 迁自有域名后改成：https://blog.example.com
